# VyPR Analysis Library Testing

In order to run the tests, access directory ``tests``, give the setup script the permission to execute ``chmod +x setup.sh`` and run the setup and testing script with ``./setup.sh``.
